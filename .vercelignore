# Vercel does not read .gitignore. Without this it uploads the source media
# masters, which are 485 MB and include a file over the 100 MB hard limit.
# Only the web ready derivatives in public/ are ever deployed.
pics/
videos/

node_modules/
.next/
out/
.git/
.env
.env.local
*.log
.DS_Store
